jdk:
  - openjdk21
maven:
  version: 3.8.5

publish:
  included:
    - api
    - data
#install:
#  - mvn -pl api,data -am install
#

build:
  scripts:
    - cd data && mvn install
    - cd api && mvn install